{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile TypeScript",
            "type": "shell",
            "command": "tsc",
            "args": ["-p", "."],  // Assurez-vous que votre tsconfig.json est à la racine de votre espace de travail
            "problemMatcher": ["$tsc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
